query PostDetails ($id:ID!) {
    post(id: $id) {
        name
        slug
        tagline
        description
        votesCount
        user { # hunter
            id
            username
        }
        makers {
            id
            name
            username
        }
        thumbnail{
            type
            url
            videoUrl
        }
        media { # TODO only images not video
            type
            url
            videoUrl
        }
    }
}